import vueConfig from "./vue.js";

/**
 * Конфигурация для Nuxt-приложений.
 *
 * Отличий от базовой немного: Nuxt авто-импортирует ref/computed/useRoute и
 * компоненты, поэтому проверка «объявлено ли имя» здесь бессмысленна вдвойне —
 * она уже выключена в базовой конфигурации, а типы приходят из `.nuxt/`.
 */
export default [
  ...vueConfig,
  {
    ignores: ["**/.nuxt/**", "**/.output/**", "**/dist/**"],
  },
];
