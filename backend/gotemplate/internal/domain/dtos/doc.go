// Package dtos — единственный слой форм между транспортом и сценарием.
//
// Здесь живут и входные структуры (Input/Patch — их связывают напрямую и HTTP
// body, и внутренние вызовы), и представления (View).
//
// Чего здесь намеренно нет:
//
//   - отдельных request-структур в контроллерах: HTTP-тело связывается прямо в
//     dtos.*Input;
//   - пакета internal/domain/models с копией строки БД: sqlc уже отдаёт
//     нативные Go-типы, и такой «домен» оказывается маппингом поля в поле того
//     же типа.
//
// Одна форма, описанная в четырёх местах, — основная цена, которую этот шаблон
// не платит. Модель заводится только при доказанном расхождении доменной формы
// со строкой БД, и причина фиксируется в ADR.
package dtos
